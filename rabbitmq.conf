# تنظیم heartbeat پیش‌فرض سرور به ۶۰ ثانیه.
# اگر کلاینت (Celery) مقدار کمتری مثل ۳۰ ثانیه درخواست کند، روی ۳۰ ثانیه توافق خواهد شد.
# این مقدار باید بزرگتر یا مساوی مقداری باشد که در Celery تنظیم می‌کنید،
# یا حداقل به اندازه‌ای باشد که از timeout های شبکه جلوگیری کند.
heartbeat = 60

# (اختیاری اما خوب است) برای مشاهده لاگ‌های RabbitMQ، مسیر لاگ را مشخص کنید:
# log.file = /var/log/rabbitmq/rabbit.log
# log.dir  = /var/log/rabbitmq
# توجه: دایرکتوری /var/log/rabbitmq باید توسط کاربر rabbitmq قابل نوشتن باشد.
# در ایمیج‌های داکر، لاگ‌ها معمولا به stdout می‌روند و نیازی به این خطوط نیست.